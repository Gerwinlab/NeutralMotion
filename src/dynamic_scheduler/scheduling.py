#TODO: write code that treats the problem as a dynamic problem what has a length(dag) x sqrt(N) x sqrt(N) x 2^n matrix where n is number is future time steps.
#TODO: When applying gates do so such that two pulse are applied so there is two pulse. If they are separated by whole row then it is fine. Ensure no overlap of gates.
#TODO: convert DAG into N DAGs that contain hold/wait dependencies telling it which other DAGs much finish before it can continue.
